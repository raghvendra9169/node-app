version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: demo-tdf
        LoadBalancerInfo:
          ContainerName: "node-app"
          ContainerPort: 3000
